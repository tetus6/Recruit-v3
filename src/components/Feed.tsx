import React from "react";
import { api } from "~/utils/api";

const Feed = () => {
  // const { registration } = api.registration.fetch.useQuery();

  return <div>Feed</div>;
};

export default Feed;
